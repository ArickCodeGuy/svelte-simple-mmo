import type { MapCellType } from '../types';

const cellTypeProto: MapCellType = {
  name: 'Fountain',
  icon: 'water',
  actions: [
    {
      name: 'Drink',
    },
  ],
};

export default cellTypeProto;
