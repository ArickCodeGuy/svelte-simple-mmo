import type { LivingCompleteStats } from '../types';

export const useLivingCompleteStats = (): LivingCompleteStats => ({
  attack: 0,
  health: 0,
  dexterity: 0,
  intelligence: 0,
  mind: 0,
  strength: 0,
  vitality: 0,
});
